import confetti from 'canvas-confetti'\nimport { CONFETTI_CONFIG } from '../utils/constants'\n\nexport function useConfetti() {\n  let canvasConfetti = null\n  \n  const initializeConfetti = (canvas) => {\n    if (!canvas) return\n    \n    canvasConfetti = confetti.create(canvas, {\n      resize: true,\n      useWorker: true\n    })\n  }\n  \n  const showConfetti = () => {\n    if (!canvasConfetti) return\n    \n    // Multiple bursts for better effect\n    CONFETTI_CONFIG.BURST_SEQUENCE.forEach((burst, index) => {\n      setTimeout(() => {\n        canvasConfetti({\n          particleCount: burst.particleCount,\n          spread: burst.spread,\n          origin: burst.origin,\n          colors: CONFETTI_CONFIG.COLORS\n        })\n      }, index * CONFETTI_CONFIG.BURST_DELAY)\n    })\n  }\n  \n  return {\n    initializeConfetti,\n    showConfetti\n  }\n}"